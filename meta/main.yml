---
dependencies: []

galaxy_info:
  author: reminec
  description: Install firewall/Router service wich use configurable iptables rules. Default policy to DROP INPUT/OUTPUT/FORWARD (Fork of geerlingguy.firewall)
  company: "reminec"
  license: "license (BSD, MIT)"
  min_ansible_version: 1.7.2
  platforms:
  - name: EL
    - all
    versions:
  - name: Debian
    versions:
    - all
  - name: Ubuntu
    versions:
    - all
  galaxy_tags:
    - networking
    - iptables
    - firewall
    - router
